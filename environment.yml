name: mcdo-vlm-uncertainty
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch=2.1.2
  - torchvision=0.16.2
  - pytorch-cuda=11.8  # remove or adjust if on CPU-only
  - pip
  - pip:
      - open_clip_torch==2.24.0
      - pillow==10.2.0
      - numpy==1.26.4
      - tqdm==4.66.2
      - scipy==1.11.4
      - networkx==3.2.1
      - transformers==4.51.3
